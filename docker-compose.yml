services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: petis-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - petis-network

networks:
  petis-network:
    driver: bridge
